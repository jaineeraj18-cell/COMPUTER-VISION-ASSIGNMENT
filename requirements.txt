# Runtime
playwright==1.56.0        # browser actuator (surface/web.py)
anthropic>=0.40.0         # discovery loop only; never imported by replay
PyYAML>=6.0               # policy + outcome rule library
Flask>=3.0                # mock operator console + tenant-variant app

# Dev
pytest>=8.0
